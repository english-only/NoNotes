import { SettingsPage as SettingsPageContent } from "@/app/(workspace)/settings/_components/settings-page";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return <SettingsPageContent />;
}
